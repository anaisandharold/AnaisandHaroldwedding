const gate=document.querySelector('#gate');const formAccess=document.querySelector('#access-form');const codeInput=document.querySelector('#access-code');const accessStatus=document.querySelector('#access-status');const CODES={PREMIERE2027:'full',VAUDEVILLE2027:'limited'};let accessLevel=null;function applyAccess(level){accessLevel=level;document.body.dataset.access=level;document.querySelectorAll('[data-access="full"]').forEach(el=>{const hide=level!=='full';el.hidden=hide;el.querySelectorAll('input').forEach(i=>{i.disabled=hide;if(hide)i.checked=false})});document.querySelectorAll('.access-sensitive').forEach(el=>el.textContent=level==='full'?el.dataset.fullText:el.dataset.limitedText);gate.classList.add('opened');document.body.classList.remove('locked');sessionStorage.setItem('weddingAccess',level)}formAccess.addEventListener('submit',e=>{e.preventDefault();const code=codeInput.value.trim().toUpperCase().replace(/\s+/g,'');if(!CODES[code]){accessStatus.textContent='That code is not recognised. Please check your invitation.';return}accessStatus.textContent='';applyAccess(CODES[code])});const saved=sessionStorage.getItem('weddingAccess');if(saved==='full'||saved==='limited')applyAccess(saved);const menu=document.querySelector('.menu-toggle');const nav=document.querySelector('#site-nav');menu.addEventListener('click',()=>{const open=nav.classList.toggle('open');menu.setAttribute('aria-expanded',String(open))});document.querySelectorAll('#site-nav a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));document.querySelectorAll('input[data-group]').forEach(input=>input.addEventListener('change',()=>{if(!input.checked)return;const peers=[...document.querySelectorAll(`input[data-group="${input.dataset.group}"]:not(:disabled)`)] ;if(input.hasAttribute('data-decline'))peers.filter(p=>p!==input).forEach(p=>p.checked=false);else peers.filter(p=>p.hasAttribute('data-decline')).forEach(p=>p.checked=false)}));const rsvp=document.querySelector('#rsvp-form');const formStatus=document.querySelector('#form-status');rsvp.addEventListener('submit',e=>{e.preventDefault();const data=new FormData(rsvp);const day15=data.getAll('day15');const day16=data.getAll('day16');if(!day15.length){formStatus.textContent='Please select at least one answer for 15 January.';return}if(accessLevel==='full'&&!day16.length){formStatus.textContent='Please select at least one answer for 16 January.';return}const lines=[`Invitation type: ${accessLevel==='full'?'Full weekend':'Church, reception & party'}`,`Name: ${data.get('name')}`,`Email: ${data.get('email')}`,'','Friday 15 January:',...day15.map(x=>`- ${x}`)];if(accessLevel==='full'){lines.push('','Saturday 16 January:',...day16.map(x=>`- ${x}`),'',`Meal preference: ${data.get('diet')||'Not specified'}`)}lines.push('','2027 resolution:',data.get('message')||'—');window.location.href=`mailto:anaisetharoldgrosjean@gmail.com?subject=${encodeURIComponent(`Wedding RSVP — ${data.get('name')}`)}&body=${encodeURIComponent(lines.join('\n'))}`;formStatus.textContent='Your email app should open with the RSVP ready to send.'});
